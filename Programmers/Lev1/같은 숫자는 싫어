function solution(arr) {
    var ans = []
    ans.push(arr[0])

    for(var i = 1; i < arr.length; i++) {
        if(ans[ans.length - 1] != arr[i]) {
            ans.push(arr[i])
        }
    }
    return ans
}
